% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean-data.R
\name{clean_data}
\alias{clean_data}
\title{Limpia datos oceanográficos eliminando registros en tierra}
\usage{
clean_data(data_raw, output_file, shp_mask = NULL)
}
\arguments{
\item{data_raw}{Data frame con columnas \code{lon} y \code{lat} en coordenadas
geográficas (CRS 4326).}

\item{output_file}{Ruta del archivo CSV de salida (e.g.
\code{"data/clean/datos_limpios.csv"}).}

\item{shp_mask}{Objeto \code{sf} con la máscara terrestre. Si es \code{NULL}, se
descarga automáticamente una máscara de baja resolución mediante
\code{\link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries()}}.}
}
\value{
Data frame limpio (sin geometría) con las mismas columnas que
\code{data_raw} más \code{lon} y \code{lat} actualizadas, exportado además como CSV en
\code{output_file}.
}
\description{
Convierte un data frame con coordenadas a objeto \code{sf}, elimina los puntos
que intersectan con una máscara terrestre y exporta el resultado como
archivo CSV con columnas \code{lon} y \code{lat} restauradas.
}
\details{
La función ejecuta los siguientes pasos:
\enumerate{
\item Prepara la máscara terrestre (repara geometrías y disuelve polígonos).
\item Convierte \code{data_raw} a objeto \code{sf} y reproyecta al CRS de la máscara.
\item Elimina los puntos que intersectan con la máscara terrestre.
\item Recupera las coordenadas proyectadas como columnas \code{lon} / \code{lat}.
\item Exporta el resultado a \code{output_file}.
}
}
\note{
Si \code{shp_mask} es \code{NULL} se requiere conexión a internet para descargar
la máscara desde Natural Earth.
}
\examples{
\dontrun{
shp_mask <- sf::st_read("Golfo_Arauco_prj2.shp")

clean_data(
  data_raw    = mis_datos,
  output_file = "data/clean/datos_limpios.csv",
  shp_mask    = shp_mask
)
}
}
\seealso{
\code{\link[sf:geos_binary_pred]{sf::st_intersects()}}, \code{\link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries()}}
}
