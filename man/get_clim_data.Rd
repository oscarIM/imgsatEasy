% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-clim-data.R
\name{get_clim_data}
\alias{get_clim_data}
\title{get_clim_data}
\usage{
get_clim_data(
  dir_input,
  season,
  stat_function,
  var_name,
  start_date = NULL,
  end_date = NULL,
  xlim,
  ylim,
  n_cores = 1,
  n_day_min = 3,
  year_min = 3,
  name_output = NULL
)
}
\arguments{
\item{dir_input}{Ruta al directorio que contiene los archivos de entrada
(.parquet o .csv). Los nombres de archivo deben comenzar con la fecha
en formato \code{YYYY-MM-DD} o \code{YYYY-MM}.}

\item{season}{Temporalidad de agregación. Opciones: \code{"month"},
\code{"week"} o \code{"year"}.}

\item{stat_function}{Nombre de la función estadística a aplicar como
string. Ejemplos: \code{"median"}, \code{"mean"}.}

\item{var_name}{Nombre de la variable a procesar tal como aparece en los
archivos de entrada. Ejemplos: \code{"chlor_a"}, \code{"sst"},
\code{"Rrs_645"}, \code{"Kd490"}.}

\item{start_date}{Fecha de inicio del periodo a procesar en formato
\code{"YYYY-MM-DD"}. Si es \code{NULL} se usan todos los archivos
disponibles.}

\item{end_date}{Fecha de término del periodo a procesar en formato
\code{"YYYY-MM-DD"}. Si es \code{NULL} se usan todos los archivos
disponibles.}

\item{xlim}{Vector numérico de longitud 2 con los límites de longitud
\code{c(lon_min, lon_max)}.}

\item{ylim}{Vector numérico de longitud 2 con los límites de latitud
\code{c(lat_min, lat_max)}.}

\item{n_cores}{Número de núcleos a utilizar para el procesamiento
paralelo. Por defecto \code{1} (procesamiento secuencial). El
paralelismo usa \code{parallel::makeForkCluster} y solo está
disponible en sistemas Unix/macOS.}

\item{n_day_min}{numero d días min con valores válidos}

\item{year_min}{numero min de años con valores válidos}

\item{name_output}{Ruta y nombre del archivo CSV de salida. Si es
\code{NULL} (por defecto) no se guarda ningún archivo y la función
retorna el data frame de forma invisible.}
}
\value{
Data frame con las columnas \code{lat}, \code{lon}, \code{fill}
(valor de la estadística calculada) y \code{season} (factor ordenado
con el nombre del periodo: nombre del mes en español, \emph{Semana N}
o el año). Si \code{name_output} no es \code{NULL}, el data frame se
guarda en disco como CSV y se retorna de forma invisible.
}
\description{
Procesa archivos satelitales (parquet o csv) y genera un
data frame con la estadística resumen por temporada (mes, semana o año)
para una variable oceanográfica dada. El resultado puede guardarse como
CSV y usarse directamente en \code{\link{plot_clim}}.
}
\examples{
\dontrun{
get_clim_data(
  dir_input = "/datos/satelital/sst/parquet",
  season = "month",
  stat_function = "median",
  var_name = "sst",
  start_date = "2020-01-01",
  end_date = "2023-12-31",
  xlim = c(-76, -70),
  ylim = c(-40, -35),
  n_day_min = 3,
  year_min = 3,
  n_cores = 8,
  name_output = "sst_mensual.csv"
)
}
}
\seealso{
\code{\link{plot_clim}} para graficar el resultado de esta función.
}
