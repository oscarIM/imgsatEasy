% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-time-serie-oce.R
\name{get_time_serie}
\alias{get_time_serie}
\title{Generar serie de tiempo a partir de datos satelitales}
\usage{
get_time_serie(
  files,
  stat_function,
  file_type,
  var_name,
  shp_mask = NULL,
  n_cores = 1,
  output_name
)
}
\arguments{
\item{files}{Character vector. Rutas a los archivos de entrada
(\code{.parquet} o \code{.csv}).}

\item{stat_function}{Character. Nombre de la stat_functionión de agregación principal
(e.g. \code{"mean"}, \code{"median"}).}

\item{file_type}{Character. Tipo de archivo: \code{"parquet"} o \code{"csv"}.}

\item{var_name}{Character. Nombre de la var_name objetivo presente en los
archivos (e.g. \code{"sst"}, \code{"chlor_a"}).}

\item{shp_mask}{Objeto \code{sf} (opcional). Polígono(s) de tierra para filtrar
puntos terrestres. Si es \code{NULL} (por defecto), se descarga la capa
\code{ne_countries(scale = "large")} de \pkg{rnaturalearth}.}

\item{n_cores}{Integer. Número de núcleos para procesamiento paralelo.
Si \code{n_cores = 1} (por defecto), se ejecuta en modo secuencial.}

\item{output_name}{Character. Ruta completa del archivo CSV de salida
(e.g. \code{"output/serie_sst.csv"}). Se crean las carpetas intermedias si
no existen.}
}
\value{
Invisible. Escribe un CSV en \code{output_name} con las columnas:
\code{fecha}, \code{<var_name>_<stat_function>}, \code{<var_name>_min},
\code{<var_name>_max}, \code{<var_name>_sd}.
}
\description{
Lee archivos parquet o CSV con datos georreferenciados, elimina los puntos
terrestres mediante una máscara costera y resume la var_name indicada por
fecha, calculando media (u otra stat_functionión), mínimo, máximo y desviación
estándar.
}
\details{
La lectura de archivos utiliza \pkg{arrow} para ambos formatos (parquet y
CSV), aprovechando la evaluación lazy (pushdown de \code{select} y \code{filter})
para minimizar el uso de memoria. Solo se cargan las columnas necesarias
(\code{lat}, \code{lon}, \code{date1} y la var_name objetivo) y se descartan filas con
\code{NA} en la var_name antes de traer los datos a R.
}
\examples{
\dontrun{
files <- list.files("data/sst/", pattern = "\\\\.parquet$", full.names = TRUE)

get_time_serie(
files = files_parquet,
stat_function = "median",
file_type = "parquet",
var_name = var_name,
shp_mask = shp_mask,
n_cores = 2,
output_name = glue::glue("sst_GA_{periodo_clim}_{sensor}_ts_clean.csv"))
)
}
}
