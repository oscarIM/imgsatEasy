% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-wind-data.R
\name{get_wind_data}
\alias{get_wind_data}
\title{Descarga y procesamiento de datos de viento desde ERA5}
\usage{
get_wind_data(
  long_min,
  long_max,
  lat_min,
  lat_max,
  start_time,
  end_time,
  dir_output,
  prefix_outfile,
  product_name
)
}
\arguments{
\item{long_min}{Longitud mínima del área de interés (decimal).}

\item{long_max}{Longitud máxima del área de interés (decimal).}

\item{lat_min}{Latitud mínima del área de interés (decimal).}

\item{lat_max}{Latitud máxima del área de interés (decimal).}

\item{start_time}{Fecha de inicio en formato \code{"YYYY-MM-DD"}.}

\item{end_time}{Fecha de término en formato \code{"YYYY-MM-DD"}.}

\item{dir_output}{Ruta del directorio donde se guardarán los archivos descargados y procesados.}

\item{prefix_outfile}{Prefijo que se usará para nombrar los archivos de salida (\code{.nc} y \code{.csv}).}

\item{product_name}{Nombre del producto a solicitar: \code{"pressure_levels"} para viento a 1000 hPa o \code{"single-levels"} para viento a 10 metros de altura.}
}
\value{
Exporta un archivo CSV con los datos diarios promedio de viento (velocidad y dirección), y guarda el NetCDF descargado. No retorna ningún objeto en R.
}
\description{
Esta función descarga datos diarios de viento del reanálisis ERA5 (Copernicus) para un área y período definidos. Puede trabajar con datos de niveles de presión o de superficie. Los archivos se descargan en formato NetCDF y se procesan para obtener valores diarios promedio de velocidad y dirección del viento.
}
\details{
Esta función requiere que el usuario tenga configurada una cuenta en el CDS (Climate Data Store) con su clave de API, la cual puede configurarse mediante \code{ecmwfr::wf_set_key()}.
}
\examples{
\dontrun{
get_wind_data(
  long_min = -75, long_max = -70,
  lat_min = -40, lat_max = -35,
  start_time = "2023-01-01",
  end_time = "2023-01-10",
  dir_output = tempdir(),
  prefix_outfile = "wind_data",
  product_name = "single-levels"
)
}

}
